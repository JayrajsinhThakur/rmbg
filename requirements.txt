# FastAPI server
fastapi
uvicorn[standard]
python-multipart

# Core
numpy
pillow
tqdm

# Optional but commonly needed by DIS/IS-Net helpers
scikit-image
opencv-python-headless

# Hugging Face download (if you use auto-download in code)
huggingface_hub

# PyTorch CPU wheels (recommended for Render unless you use GPU elsewhere)
--index-url https://download.pytorch.org/whl/cpu
torch==2.7.0
torchvision==0.22.0